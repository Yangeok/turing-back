require('../chai')();
const { request, expect } = require('chai');
const app = require('../request');

describe('Tax', () => {
  describe('GET /tax', () => {});
  describe('GET /tax/:id', () => {});
});
