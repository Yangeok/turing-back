require('../chai')();
const { request, expect } = require('chai');
const app = require('../request');

describe('Order', () => {
  describe('POST /order', () => {});
  describe('GET /order/customer', () => {});
  describe('GET /order/detail/:id', () => {});
  describe('GET /order/:id', () => {});
});
