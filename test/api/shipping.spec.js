require('../chai')();
const { request, expect } = require('chai');
const app = require('../request');

describe('Shipping', () => {
  describe('GET /shipping/region', () => {});
  describe('GET /shipping/region/:id', () => {});
});
