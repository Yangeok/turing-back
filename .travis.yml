language: node_js
node_js:
  - 'stable'
cache:
  yarn: true
  directories:
    - 'node_modules'
services:
  - 'mysql'
addons:
  hosts:
    - localhost
env:
  - NODE_ENV=test
before_script:
  - netstat -atlnp
  - iptables -I INPUT 1 -p tcp --dport 80 -j ACCEPT
script:
  - sudo yarn test
after_script: