language: node_js
node_js:
  - 'node'
services:
  - 'mysql'
before_install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS turing_test;'
addons:
  hosts:
    - localhost
env:
  - NODE_ENV=test
cache:
  yarn: true
  directories:
    - 'node_modules'
script:
  - yarn test
