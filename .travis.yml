language: node_js
node_js:
  - 'stable'
cache:
  yarn: true
  directories:
    - 'node_modules'
services:
  - 'mysql'
addons:
  hosts:
    - localhost
env:
  - NODE_ENV=test
before_script:
  - sudo su
  - iptables -I INPUT 1 -p tcp --dport 80 -j ACCEPT
script:
  - yarn test
after_script: